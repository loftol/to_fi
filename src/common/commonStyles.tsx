import {StyleSheet} from 'react-native';

const commonStyles = StyleSheet.create({
  border: {
    borderWidth: 3,
    borderColor: '#000',
    borderStyle: 'solid',
  },
});

export default commonStyles;
